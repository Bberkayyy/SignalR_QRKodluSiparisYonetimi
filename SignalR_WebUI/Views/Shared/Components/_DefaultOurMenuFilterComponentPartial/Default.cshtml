@model IList<ResultCategoryDto>
<ul class="filters_menu">
    <li class="active" data-filter="*">Tümü</li>
    @foreach (var item in Model)
    {
        <li data-filter=".@item.name">@item.name</li>
    }
</ul>